# Backend Service

This project uses a lightweight HTTP echo container (`hashicorp/http-echo`) to simulate an API service.

No custom backend code is included, as the focus of this assignment is Kubernetes deployment, GitOps, and troubleshooting.
