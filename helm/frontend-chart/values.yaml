replicaCount: 2

image:
  repository: nginx
  tag: "stable"

service:
  type: ClusterIP
  port: 80

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "200m"
    memory: "256Mi"

readinessProbe:
  path: /
  port: 80

livenessProbe:
  path: /
  port: 80


