apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  selector:
    app: backend-broken
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 5678
  type: {{ .Values.service.type }}
